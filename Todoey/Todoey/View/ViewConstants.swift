enum ViewConstants {
    enum ReuseIdentifiers {
        static let categoryItem = "categoryItem"
        static let todoItem = "todoItem"
    }
    enum Segue {
        static let todoSegue = "showTodos"
    }
}
